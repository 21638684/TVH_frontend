export interface EventModel {
    eventId: number;    
    eventName: string; 
    capacity:number;   
    location: string;    
    startTime: string;   
    endTime: string;    
    date: string;        
    price: number;       
  }